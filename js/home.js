const {state,escapeHTML}=window.StudentHub;const s=state();studentCount.textContent=s.users.length;gigCount.textContent=s.gigs.length;projectCount.textContent=s.projects.length;const box=document.getElementById('latestGigs');box.innerHTML='';[...s.gigs].sort((a,b)=>b.createdAt-a.createdAt).slice(0,3).forEach(x=>box.innerHTML+=`<div class="card"><div class="card-top"><span class="tag">${escapeHTML(x.category||'Gig')}</span><span class="price">৳${Number(x.budget||0).toLocaleString()}</span></div><h3>${escapeHTML(x.title)}</h3><p>${escapeHTML(x.description)}</p></div>`);if(!s.gigs.length)box.innerHTML='<div class="empty">No gigs posted yet. Create an account, verify it, and post the first gig.</div>';
