const {state,save}=window.StudentHub;const form=document.getElementById('loginForm'),msg=document.getElementById('msg');form.addEventListener('submit',e=>{e.preventDefault();const s=state(),u=s.users.find(x=>x.email===email.value.trim().toLowerCase()&&x.password===password.value);if(!u){msg.textContent='Login failed: email or password is incorrect.';msg.className='notice error show';return}s.session=u.id;save(s);const next=new URLSearchParams(location.search).get('next')||'index.html';location.href=decodeURIComponent(next)});
