const {requireLogin,state,save,escapeHTML}=window.StudentHub;const statusBox=document.getElementById('statusBox'),form=document.getElementById('verifyForm'),msg=document.getElementById('verifyMsg'),type=document.getElementById('documentType'),guide=document.getElementById('verifyGuide');requireLogin(user=>{const acct=user.accountType||'student';function paint(){statusBox.className='verification-status '+user.verificationStatus;statusBox.innerHTML=`<b>Status:</b> ${escapeHTML(user.verificationStatus.toUpperCase())}${user.verificationType?' · '+escapeHTML(user.verificationType):''}`}paint();const options=acct==='student'?[['student_id','Student ID Card'],['nid','National ID (NID)']]:[['nid','National ID (NID)'],['passport','Passport']];type.innerHTML=options.map(([v,t])=>`<option value="${v}">${t}</option>`).join('');guide.textContent=acct==='student'?'Student accounts can verify using Student ID Card or NID.':'Non-student / client accounts can verify using NID or Passport.';form.onsubmit=e=>{e.preventDefault();const f=documentFile.files[0];if(!f)return;if(f.size>5*1024*1024){msg.textContent='File must be 5 MB or smaller.';msg.className='notice error show';return}const s=state(),u=s.users.find(x=>x.id===user.id);u.verificationStatus='verified';u.verificationType=type.value;u.verificationDocumentNumber=documentNumber.value.trim();u.verificationFileName=f.name;u.verificationSubmittedAt=Date.now();save(s);user.verificationStatus='verified';user.verificationType=type.value;paint();msg.textContent='Verification completed successfully for this local course-project version. You can now post gigs.';msg.className='notice success show';form.reset()}});
